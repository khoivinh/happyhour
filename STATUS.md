# Happyhour — Status

Last updated: 2026-07-21

**Status:** In progress · **Live:** <https://happyhour.day> · **Stack:** Vite + React 18 + TS + Tailwind, static (Cloudflare Pages, auto-deploys on push) · **Repo:** `khoivinh/happyhour`

World clock + timezone converter (fka Khlock). No backend; weather via Open-Meteo (no API key).

## Current state
- **2026-07-21 (Time Zone Names toggle) — new drawer toggle, default ON, swaps the GMT label for a named abbreviation (EST/CET/JST) where one exists.** ON → each tile's `GMT+X` becomes its abbreviation *if known*; unknown zones stay `GMT+X` (so the default board is a mix). Relative-time pill untouched. Abbreviations come from a curated `ZONE_ABBR` table (~40 IANA zones) in `city-lookup.ts` plus an `Intl` "short" fallback — required because `Intl` only abbreviates North-American zones (Paris/Tokyo → `GMT+2`/`+9`, not CET/JST). **DST picked from the live offset** (`stdOffset = min(jan,jul)`; in-DST when current > std), hemisphere-agnostic (Paris→CEST while Sydney→AEST). New `zoneLabel(city, showZoneAbbr)` helper feeds the 3 tile call sites; `digital-clock.tsx` untouched. Preference mirrors `showRelativeTime` (state/localStorage/drawer/cloud-sync); **default-ON init branches on `null`** so an explicit "false" still opts out, and cloud-sync/api treat a **missing field as `true`**. Backend is a real SQL Worker — added migration `0003_add_show_zone_abbr.sql` (DEFAULT 1) + threaded the column through `api/src/routes/preferences.ts`. 74 tests (2 new). **Ships in two parts: frontend on push (safe with old API); the API migration + `wrangler deploy` is a separate manual step — migrate BEFORE deploy.** → `docs/2026-07-21-zone-abbreviation-toggle-devlog.md`
- **2026-07-19 (Share View mobile fixes) — four fixes from a friend's iOS 26.5/iPhone 16 report.** (1) Removed the extraneous **full-width rule under the branding** (`LogoBar` `border-b` dropped from the sticky className), keeping only the body-width `border-t` above the headline. (2) **Weather reliability:** "WEATHER UNAVAILABLE" was a genuine Open-Meteo failure (a shared link mounts all tiles at once → burst of parallel calls; one 4G blip surfaced the error immediately since there's no loading branch) — `use-weather.ts` `retry: 1 → 3` with exponential `retryDelay`, so blips recover silently (tile stays blank, never a premature error). (3) **Blank-until-scroll:** the `<main>` min-height is now conditional — `min-h-[100dvh]` on the Sharing View (was the dashboard's `min-h-[calc(100lvh+120px)]` hero-shrink runway, dead space + a compositing trigger there). (4) **Body bleeds above the sticky header** (also on the dashboard hero): new `.sticky-layer { transform: translateZ(0) }` on the `LogoBar` sticky header + inline `translateZ(0)` on `.hero-sticky` — a GPU-layer promotion that repaints without bleed; hero-shrink verified unaffected. 72 tests (new guard: the Sharing View header now has `border-bottom-width: 0px`). Fixes 3 & 4 are iOS-compositor-specific → live QA. → `docs/2026-07-19-share-mobile-fixes-devlog.md`
- **2026-07-19 (account flow round 2) — zero default clocks, auth-aware Sharing View, Back-button fix (built on `main`, not yet pushed).** New users now start with **zero** clocks (`initZonesFromStorage` → `[]` on all paths, incl. the stored-but-empty case that used to re-seed; `DEFAULT_ZONES` gone); onboarding copy → **"Track clocks and convert time zones for up to sixteen cities"**. Sharing View is **auth-aware**: a signed-out recipient sees the blue **Registration Bar** (not the Commit Bar) with **checkless, display-only tiles** (no layout shift — `DigitalClock` `showSelectCheck`); on auth a **new registrant** is auto-added + taken to the board, a **returning** user gets the Commit Bar (told apart by `createdAt`). All Clerk usage isolated in new `shared-link-auth-controller.tsx`, so the no-Clerk test build keeps the Commit-Bar path. **Back fix:** `handleAddShared` now `pushState`s (was `replaceState`) + a `popstate` re-parse (`resolveShareFromLocation`), so Back returns to the share view. Registration Bar button **"Cancel" → "Later"**. **Two follow-ups same day:** (a) fixed the onboarding gate so a zero-clock visitor who logo-escapes a share still sees the intro (`onboarded` now needs a *non-empty* saved board, not just the key's presence — the persist effect writes `"[]"` on mount); (b) brought the **drawer icon + side panel to the Sharing View** (was hidden since round 4) — nav gate `hidden={sharePending}`, `LogoBar` gains a `sticky` prop so the branding pins on scroll (no hero there to be the sticky element), panel works as on the dashboard (Sort/Relative are no-ops on shared tiles). 72 tests (register mode + sticky-scroll are live-QA only). → `docs/2026-07-19-account-flow-round2-devlog.md`
- **2026-07-19 — Account Registration Bar (shipped + live, `cdf5f49`).** A blue bottom bar prompts a signed-out visitor to create an account once they've added a clock. Built on the `CommitBar` primitive with a theme-independent blue skin (`.registration-bar` overrides the seven `--share-bar-*` vars); "Login or Sign Up" opens the same Clerk modal the sidebar uses. **Trigger:** added-a-clock *this session* only (manual or accepting a share) — a returning signed-out user who adds nothing won't see it. **Cancel:** session-only. Both in-memory (no localStorage); signing in ends it. New `registration-bar.tsx`; `world-clock.tsx` holds the two flags + Clerk-guarded render; `time-zone-converter.tsx` gained an `onClockAdded` callback. 3 analytics events (shown/dismissed/login-clicked). 70 tests still pass — the bar is in the Clerk branch the test build omits, so it's live/manual QA. → `docs/2026-07-19-account-registration-bar-devlog.md`
- **2026-07-17 (round 5) — copy polish + Sharing View tweaks + happy-theme fix (shipped + live; `9c57f0b` then a same-day wording revert `7b8862f`).** Copy: onboarding + About now say **"time zone tool"** (was "world clock"); the **About** page heading is the tagline **"It's always that time somewhere!"** (replaces the word "About"). Sharing View: **Esc** cancels Select Mode (→ Resting); the headline is width-constrained (`sm:w-3/5`, ~60%) and reworded to **"Current time in these N cities."** / **"Time zone conversion for these N cities."**; and if the recipient **already owns every shared city**, the view skips Select Mode and lands directly in Resting (no empty Commit Bar). Privacy: the **Cookie Preferences** button is now black-on-yellow in the **happy** theme (was invisible — yellow on yellow); light/dark unchanged. 70 tests. → `docs/2026-07-17-share-round5-devlog.md`
- **2026-07-17 (round 4) — Sharing View refinements + the first edge function (shipped + live; first deploy failed on a JSON-import attribute, fixed with a self-contained `city-names.ts`).** No layout shift **Select ↔ Resting** (tiles reserve the grip slot in both); **type-aware headline** — "Current time in these N locations." (live) vs "Time zone conversion for these N locations." (custom) — that swaps on Reset/Restore; **Reset Time** reworked: its space is reserved, it's hidden in Select Mode, appears after Cancel, and toggles **Reset Time ↔ Restore Custom Time**; owned cities get an inert **"Already saved"** menu item. Board: ellipsis **"Delete" → "Remove"**. **/support** now opens at the top (global scroll-to-top on route change). **New feature:** dynamic share-link previews via the first **Cloudflare Pages Function** (`functions/_middleware.ts` + pure `buildPreview`) — `HTMLRewriter` injects per-share OG/Twitter text naming the cities (live vs custom copy), static `og.png` kept. 69 tests, both new guards mutation-tested, function validated with `wrangler pages dev`. → `docs/2026-07-17-share-round4-devlog.md`
- **2026-07-17 (round 3) — the Sharing View is now a place you keep, shipped + live.** It used to strip its URL on arrival and die on Cancel; now it persists. Refresh re-enters it; **Cancel** drops to a **Resting** state (flat live clocks, each with a ⋯ **"Save {city}"** menu) instead of tearing the view down; Save re-enters **Select Mode** with just that city checked; the **logo** is the way out (`<a href="/">`, full nav so Back restores the view); tiles tick live, and a frozen `&t=` share shows a **Reset Time** link. Plus: copy → "clocks" (kept "cities" in the search box); wordmark ink now tracks `--foreground` (matches the hero clock, not pure black); drawer icon theme-aware (fixes Happy gray + dark inversion in one move); Copy Link → inline "Copied" (no toast, bar stays); Share no longer dismisses the bar. 61 tests, both new guards mutation-tested. → `docs/2026-07-17-share-round3-devlog.md`
- **2026-07-17 — two recipient-side bugs fixed + deployed (`e492aac`).** (1) Silktide's cookie banner (z-index 99999) buried 67 of the commit bar's 86px, so **Add was unclickable for every share recipient**; the bar now steps up over it. (2) Share keys resolved against `cities-top.json` (500), so **29,981 of 30,481 (98.4%) silently failed** and the headline counted survivors; now falls back to the full set, keys verified identical across tiers. 56 tests. → `docs/2026-07-17-devlog.md`

Older entries: `STATUS-archive.md`.

## Next steps
- **Ship the Time Zone Names backend:** apply the D1 migration then deploy the Worker, **in that order** (`cd api && npx wrangler d1 execute happyhour-db --remote --file=migrations/0003_add_show_zone_abbr.sql && npx wrangler deploy`). Until then the toggle works client-side (localStorage); only cross-device sync of an *off* state waits. Then live-QA on happyhour.day: the default board reads well as a mix (letters where known, `GMT+X` otherwise), the drawer toggle flips labels, the preference persists across reload
- **Live QA the Share View mobile fixes on the friend's setup (iOS 26.5 / iPhone 16):** the shared link paints fully on first load (no blank-until-scroll), no content bleeds above the pinned branding, weather resolves on all tiles, only the body-width rule shows under the branding; on the dashboard, confirm the hero still shrinks on scroll with no bleed. Fixes 3 & 4 are iOS-compositor-specific (untestable in CI); if the bleed persists, `viewport-fit=cover` + `env(safe-area-inset-*)` is the next lever
- **Live QA account-flow round 2 + follow-ups** on a Clerk-configured build (shipped live): the signed-out Sharing View (checkless tiles + blue Registration Bar + "Later"); new-registrant auto-add vs returning-user Commit Bar; the Back-button fix; the zero-clock intro after logo-escaping a share; and the **new Sharing-View drawer** — pinned branding on scroll, panel opens/works. Register mode + sticky-scroll are Clerk-only / position-dependent (untestable in CI)
- Live QA the shipped Registration Bar (`cdf5f49`) on a Clerk build: blue skin across all three themes, the Clerk modal, session-only Cancel, share-arrival trigger, one-bottom-bar-at-a-time
- Judge round 5 live on happyhour.day — especially the About tagline heading and the happy-theme Cookie Preferences button (both hard to unit-test)
- Optional: align the **edge OG-preview copy** (`preview.ts`) to "cities" wording too (in-app headline now says "these N cities"; the preview still names cities without "these N") — not requested this round
- Share previews name only **top-500** cities; an obscure-city share previews as "N locations" — a full-set name map is a build-step follow-up
- Judge live on happyhour.day: the Reset Time **reserved-space** gap above the tiles (now always present on custom shares); whether resting tiles reading flat on Happy yellow want a subtle edge
- Older open design calls (still unresolved, now judgeable live): the locked gray check ("already added" vs disabled?); whether selected tiles need a fill tint; the "Room for one more" copy; dark's select stroke `#5c4f2a` → `#473d21` sign-off
- `share_link_opened` now re-fires on refresh (URL persists) — dedupe is a one-liner if wanted
- Share-link leftover: a key resolving in *neither* tier is still dropped silently while the headline counts survivors; and the full-set fallback costs a 2 MB fetch before paint (748ms → 1816ms — watch on a phone)
- Real-device QA: the Resting↔Select feel, the full-reload logo escape, mobile drag (dnd-kit); confirm `share_*` events land
- A11y: `SiteFooter` renders inside `<main>`, so the footer isn't a `contentinfo` landmark
- `isDropdownOpen` / `isBeingDragged` hardcode light-mode hexes with only a `dark:` variant, so Happy falls through to light — same family as the drawer token just fixed
- Cookie-modal redesign: its save button reads "Accept all" even when saving Analytics *off*; decide whether the Figma capture script (`mcp.figma.com/.../capture.js`) ships to prod
- Wire `npm test` into CI (no workflow exists); verify iOS home-screen icon + Android favicon on real devices

## Pointers
- Key files: `client/src/pages/world-clock.tsx`, `components/digital-clock.tsx`, `components/time-zone-converter.tsx`, `components/logo-bar.tsx`, `components/shared-link-view.tsx`, `components/shared-link-auth-controller.tsx`, `components/commit-bar.tsx`, `components/share-selection-bar.tsx`, `components/registration-bar.tsx`, `App.tsx` (route scroll-to-top), `functions/_middleware.ts` + `functions/lib/preview.ts` (edge previews)
- Sharing View auth modes: a signed-out recipient gets **register mode** — `shared-link-auth-controller.tsx` (Clerk-only, mounted from `world-clock` when `isClerkConfigured`) computes `registerMode = !isSignedIn` and passes it to `SharedLinkView`, which then shows the blue `RegistrationBar` instead of the Commit Bar and renders **display-only tiles** (`DigitalClock showSelectCheck={false}`, no `onToggleSelect` — the tap layer and check hide but the geometry stays). On the ref-latched sign-in edge the controller branches on `user.createdAt`: new registrant → `onAdd(all keys)` (board + highlight), returning → no-op (Commit Bar reveals; `SharedLinkView` re-derives its pre-check). The no-Clerk test build renders the plain `SharedLinkView` (Commit Bar), so register mode is **live-QA only**.
- Registration Bar: `registration-bar.tsx` reuses `CommitBar`; the blue theme-independent skin is a `.registration-bar` wrapper in `index.css` overriding the seven `--share-bar-*` vars (they cascade into CommitBar's fixed child by DOM ancestry). Visibility is decided in `world-clock.tsx` from two in-memory flags (`addedThisSession`, `regDismissed`) — set by `handleAddShared` (added>0) and `TimeZoneConverter`'s `onClockAdded`; gated on `isClerkConfigured` so `useAuth` never runs without a provider (⇒ invisible in the Clerk-less test build). "Login or Sign Up" wraps Clerk's `SignInButton mode="modal"`, which *composes* the child onClick (child runs, then modal opens). Button says **"Later"** (not "Cancel"). The **same bar** is reused in the signed-out Sharing View (see the Sharing View auth-modes pointer).
- Tests: `npm test` (Playwright, `tests/`). Runs against a **prod build** — `analytics.ts` no-ops unless `import.meta.env.PROD`, so a dev server can't exercise analytics or the consent gate. Analytics assertions read `window.dataLayer`. Builds with `VITE_CLERK_PUBLISHABLE_KEY=` empty on purpose: a local `.env` key would compile in and mount `<ClerkProvider>`, so CI would test a different `App.tsx` branch than your machine.
- Sharing View states: `SharedLinkView` holds a local `mode: 'select' | 'resting'`. Select = checks + Commit Bar (arrival + post-Save); Resting = ⋯ menu ("Save {city}", or inert "Already saved" for owned cities), no bar. Arrival is Select — **except when the recipient already owns every shared city** (`newKeys.length === 0`), which starts in Resting (no empty bar). Cancel → Resting (view stays; `onDismiss` is analytics-only). **Esc** = Cancel (a `document` keydown guarded on `mode === 'select'`). Tiles pass `reserveGripSlot` so the content column doesn't shift Select↔Resting. Headline is type-aware (`headlineFor(count, !frozen)`); the Reset Time link's slot is reserved when `t != null`, hidden in Select Mode, and toggles frozen↔live (`toggleTime`) — label "Reset Time" ↔ "Restore Custom Time". The `?z=`/`?t=` params **persist** (no longer stripped on mount) so refresh re-enters. **Back/Forward (2026-07-19):** `handleAddShared` now `pushState`s the clean URL on top of the `?z=` entry (was `replaceState`), and a `popstate` listener re-runs `resolveShareFromLocation()` (the extracted mount parse, token-guarded against stale async), so Back returns to the share view and Forward to the board — a pure SPA transition. The logo escape is still a full `<a href="/">` reload (unaffected).
- Share previews (edge): the **first Pages Function on the HTML path** — `functions/_middleware.ts` rewrites per-share OG/Twitter meta via `HTMLRewriter` when `?z=` is present (else passes through); copy from the pure, unit-tested `functions/lib/preview.ts` `buildPreview(z, t)`, which replays the client key algorithm against `cities-top.json`. Image stays static `og.png`. `functions/` isn't in the app tsc `include`, so it never typechecks in `tsc --noEmit`; validate with `wrangler pages dev dist` + curl. **Deploy:** the Pages project's root dir must contain `functions/` or it silently no-ops.
- Share flow: link schema `/?z=<keys>[&t=<epoch ms>]` (`URLSearchParams` encodes commas as `%2C`; round-trips). **Sending:** select mode + link build in `time-zone-converter.tsx`; menu + tile selection in `digital-clock.tsx`. `DigitalClock` ellipsis menu renders when `!isSelectMode && (onShare || onSave)` — the board passes `onShare`/`onRemove`, the Resting view passes only `onSave`.
- Strokes: judge a tile stroke against **its own fill**, never the page. Use `cn()` for the tile's class chain: `border-transparent` and a state's `border-[…]` have equal specificity, so plain interpolation lets source order win and Tailwind emits `border-transparent` last. `tests/share-selection.spec.ts` guards it.
- City tiers: keys are **generated at runtime** by collision resolution, not stored — but identical in both tiers (verified 500/500), because `cities-top.json` is a population-ordered *prefix* of `cities.json`. Never resolve a user-supplied key against the top 500 alone (it's 500 of 30,481).
- The bottom of the viewport is shared: `SiteFooter`, the `CommitBar`, **and Silktide's cookie banner** (`#stcm-banner`, z-index 99999, always up for a first-time visitor — which every share recipient is). The bar steps up over it via `useConsentBannerHeight`. Anything new pinned to the bottom must account for all three.
- Timed UI in tests: `page.clock.install()` does **not** freeze time — use `install()` + `pauseAt(Date.now() + 60_000)`. Settled-position assertions must poll (`toPass`/`expect.poll`); a single read lands mid-transition.
- Header geometry is one interlocked system: `TOGGLE_TOP` (world-clock.tsx) sets the drawer icon **and** the menu panel; `LogoBar`'s padding and `.hero-clock`'s padding-top share a baseline. Change one, re-measure all four.
- Drawer on the Sharing View (2026-07-19): the drawer nav is now gated `hidden={sharePending}` (was also hidden on `shareImport` — the round-4 "no drawer here" is reversed). `LogoBar` takes a `sticky` prop (`sticky top-0 z-40 sticky-layer` when set, else scroll-away — **no bottom rule of its own** as of 2026-07-19, since the body-width rule above the headline is the only divider; `.sticky-layer` is a `translateZ(0)` GPU-layer promotion that fixes an iOS Safari sticky-repaint bleed); `world-clock` passes `sticky={Boolean(shareImport)}` so the branding pins on scroll in the Sharing View (which has no hero to be the sticky element). The fixed drawer icon at `TOGGLE_TOP` aligns to the pinned wordmark's 32px ink-top. The Sharing View's `<main>` uses `min-h-[100dvh]` (not the dashboard's `lvh`+120 hero-shrink runway). Sort/Show-Relative don't reach `SharedLinkView` (silent no-ops on shared tiles); **`showZoneAbbr` does** reach it (threaded like `use24Hour`, through `shared-link-auth-controller.tsx` too), so shared tiles honor the recipient's Time Zone Names preference.
- Design doc: `docs/PRD.md` · Devlogs: `docs/` (latest 2026-07-17 round 4) · Branch previews: `<branch>.khlock.pages.dev` (Pages project still named `khlock`, *not* a `happyhour.day` subdomain).
