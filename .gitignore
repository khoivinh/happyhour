node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
docs/HANDOFF.md

# iCloud conflict-copy artifacts
* 2.md
* 2.ts
* 2.sql
* 2/

# Local-only working directories + Wrangler state
.agents/
chat/
design/
api/.wrangler/
api/api

# Change-request specs authored per-session (kept locally, not in repo)
docs/*-happyhour-changes-*.md

# Real dependency dir; iCloud skips any path ending in .nosync
node_modules.nosync/

# Playwright run artifacts (traces, screenshots, HTML report)
test-results/
playwright-report/
