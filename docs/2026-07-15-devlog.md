# 2026-07-15 — Onboarding tagline above "Add Time Zone"

Spec: `Scratch/2026-07-15-happyhour-edits.md`. Figma: `329:3241` (frame), `338:3813` (tagline node).
All changes in `client/src/components/time-zone-converter.tsx`. Shipped to `main`.

## Why

New visitors don't immediately grasp what Happyhour is. Added a one-time onboarding headline —
"Welcome to the indispensable world clock." — above the "Add Time Zone" button. On the user's
first-ever add it fades and the button + tile grid animate up into place, then never returns.

## The empty-state problem

The spec's trigger ("after the user adds her first custom clock tile") assumes an empty start, but
the app is **never empty**: it seeds 3 default tiles (`DEFAULT_ZONES`) and `handleRemoveClock` refuses
to drop below 1. Khoi's call: gate on a persistent "has ever added a zone" flag and keep the seeded
tiles (this matches the Figma frame, which shows the tagline sitting above populated tiles).

## What changed

| Concern | Implementation |
|---|---|
| Flag | `ONBOARDED_KEY = "world-happyhour-onboarded"`. `onboarded` init = flag set **or** a `world-happyhour-zones` key already exists — so returning users are silently onboarded and never see it; only a genuinely new visitor does. |
| Trigger | In `handleAddClock`, on the first successful add: persist the flag immediately (survives a mid-animation refresh), then `setTaglineHiding(true)` (or, under reduced motion, `setOnboarded(true)` to cut instantly). |
| Unmount | `onTransitionEnd` fires on the `grid-template-rows` collapse (ignoring the earlier opacity fade) → `setOnboarded(true)`. |
| Tagline node | `338:3813`: `font-display font-black text-foreground text-[36px] leading-[38px] tracking-[-1px]`, `w-3/5` (60% of the `max-w-4xl` column), `px-[10px]` to align left with the button. Natural wrap, no forced `<br>`. |

## The reflow, without a JS animation lib

No framer-motion in the project. The tagline is wrapped in a CSS grid whose single row animates
`grid-rows-[1fr] → grid-rows-[0fr]`; the `overflow-hidden min-h-0` child collapses to zero height and
the add button + tile grid, being later siblings in normal flow, **slide up continuously** as the row
interpolates — the "elegantly animate up" for free, no FLIP/measuring. Two-phase feel via per-property
transition timing: `[transition-duration:180ms,360ms] [transition-delay:0ms,150ms]` over
`[opacity,grid-template-rows]` — opacity fades first (short), then the row collapses (longer, delayed).
`motion-reduce:transition-none` handles reduced motion.

## Verification

`tsc --noEmit` + `npm run build` clean. Playwright (25 assertions, all pass):

- **Spec:** Zalando Black / weight 900 / 36px / 38px / −1px, color == hero time display in light+dark+happy,
  width ratio 0.600 (525.6/876), left edge x=202 == button, no `<br>`, wraps to 2 lines desktop.
- **First add:** opacity → 0 mid-animation, flag persists immediately, tagline unmounts, tile grid slid up
  498→390, stays gone after reload.
- **Returning user** (zones key present, no flag): no tagline.
- **Reduced motion:** cuts instantly, flag still persists.

## Mobile line breaks (follow-up, same day)

Figma is desktop-only; at 375px the 60% width + 36px broke the headline into ~5 short lines. Fix per
Khoi: let the tagline span the **full body width** on mobile, keep 60% on desktop — `w-3/5` →
`w-full sm:w-3/5`. Now 3 balanced lines at 375px (full width), unchanged 2 lines / 60% at desktop.

## Location hint → Support instructions (same day)

The footer's dead "Allow location for a closer match." (shown on `geoDenied`) now helps the user fix it.
**Browser reality that shaped the design:** once location is *hard-blocked*, JS can't reopen the native
prompt (only browser site-settings can), and on iOS Safari the mount-time request often never prompts at
all. A click-to-reprompt would fail for exactly the blocked users who need it — so instead we route to a
Support section with per-browser instructions, which works in every state. No geolocation code touched.

| File | Change |
|---|---|
| `components/site-footer.tsx` | Hint copy → "Allow Happyhour to **access your location** for more precise local time and weather." with "access your location" a wouter `<Link href="/support">` (underline, hover→foreground). Still gated on `geoDenied`. |
| `pages/support.tsx` | New "Location access" section: intro + Safari / Chrome / Firefox blocks, each browser name linking to its canonical help page. Copy is verbatim from Khoi's `Scratch/2026-07-15-happyhour-location.md`. |

Copy went through three review rounds with Khoi, all on instruction *accuracy*: (1) don't send users to
System Settings when Safari has a per-site path; (2) the Chrome/Firefox steps were desktop-only and didn't
work on iPhone; (3) final copy supplied by Khoi. Key correctness point: **on iPhone, Chrome and Firefox
inherit the OS location permission** (Settings → Apps → Chrome/Firefox → Location), they don't expose a
per-site toggle in-app like desktop. Each entry now leads with the iPhone path. Safari link normalized off
a `…/en-sa/…` locale to the locale-neutral canonical. All three help URLs verified 200.

Verified: `tsc`/`build` clean; 24 Playwright assertions (footer copy + `/support` link + navigation; the
three canonical hrefs with `target=_blank rel=noopener noreferrer`; iPhone-path wording present; content
pages `/about`+`/privacy`+`/support` render **no** geo hint; link legible in light/dark/happy).

## Next

- Real-device QA of the fade+reflow feel (it's a feel thing, not a screenshot thing).
- Real-device check that the per-browser location instructions match current iOS/browser UI.

---

# 2026-07-15 — Share flow (tile menu → multi-select → share link)

Plan: `~/.claude/plans/review-this-obsidian-document-snuggly-brook.md`. New files:
`components/share-selection-bar.tsx`. Touched: `digital-clock.tsx`, `time-zone-converter.tsx`,
`pages/world-clock.tsx`, `index.css`. Greenfield — no sharing/select/clipboard/URL-param code existed.

## Why

The clock-tile circle-ellipsis (`…`) did exactly one thing — open a "Remove {city}?" dialog. It now opens
a two-item **menu** (Share / Delete). Share drops the grid into a multi-select mode so the user can pick
more cities, then commit a `happyhour.day` link to the OS share sheet (clipboard + toast fallback on
desktop). Opening such a link shows a preview/confirm before adding the cities.

## What shipped

| Surface | Implementation |
|---|---|
| **Tile menu** (`digital-clock.tsx`) | New `onShare` prop; the `…` button is now a shadcn `Popover` with **Share {city}** (always) + **Delete {city}** (only when removable, so hidden on the last tile). Opening it drives the existing `isDropdownOpen` so the tile shows its active tint. Testids `button-tile-menu-*`, `menu-share-*`, `menu-delete-*`. |
| **Select mode** (`digital-clock.tsx` + `SortableClockItem`) | `isSelectMode`/`isSelected`/`onToggleSelect` props: a full-tile tap overlay toggles inclusion (inner controls inert), a `primary`-colored check badge marks selection, drag is disabled. |
| **Bottom bar** (`share-selection-bar.tsx`) | Fixed, content-aligned bar: "Include my local time" checkbox + Cancel + **Share N** (disabled → "Share" at 0). State (`shareMode`, `shareSelection`, `includeLocal`) lives in the converter. |
| **Share encoding** | `commitShare()` builds `/?z=<keys>` (local city prepended when the box is checked, deduped) and appends `&t=<epoch>` **only in custom-time mode** (freeze-only-if-custom). `navigator.share` → clipboard+toast fallback, in try/catch (native-cancel is a no-op). |
| **Incoming link** (`world-clock.tsx`) | Parses `?z=`/`?t=` on load, strips the URL immediately (no re-prompt), resolves keys **after `loadTopCities()`** (see bug below), and shows a preview `Dialog` ("Add {cities} to your clocks[ at {time}]?"). Add merges (dedupe, 16-cap) + restores custom time; both choices clear the params. |

## Design gate (Apple-caliber critique loop)

Before writing any behavior, built a static mockup of all four surfaces × light/dark/happy × mobile/desktop
from the real tile tokens, screenshotted it, and ran a 2-critic panel (Happyhour-fidelity + Apple-HIG) to
consensus. Round 1 (both `revise`) → fixes: menu's parent tile now tints while open; dropped the redundant
"N selected" (count lives on the button); unified the selection badge with the bar checkbox color
(`primary`/`primary-foreground` → blue in light/dark, black+yellow in Happy) so the whole flow shares one
control color; added the 0-selected disabled state; Happy dialog uses the white `--card` surface;
"Dismiss"→"Cancel". Round 2 → **both `pass`**. A final panel on the *real build* also passed (no drift).
Locked spec: `scratchpad/share-design-spec.md`.

New CSS tokens `--tile-sel-bg` / `--tile-sel-border` per theme (`index.css`) — Happy gets a deepened gold
(`#fff0a8`/`#eab700`) since the light cream would vanish on the yellow page.

## Bugs caught during verification

- **Incoming links dropped every city** — the parse effect ran before the city lookup finished loading, so
  `getCityByKey` returned undefined for all keys. Fixed by resolving inside `loadTopCities().then(...)`.
- **Confirm-dialog copy fractured** — `formatCityDisplay` appends provinces ("Paris, Île-de-France"), so the
  comma-joined sentence read like extra items. Switched the dialog list to bare `.name`.
- **Bar/footer overlap** (Apple critic nit) — the fixed bar covered the footer at max scroll; added a
  `body { padding-bottom }` while in share mode so the footer clears it.

## Verification

`tsc` + `build` clean. 26 Playwright assertions (`scratchpad/verify-share.mjs`): menu rows + last-tile
Delete-hidden; select enter/toggle/cancel + 0-state disable + Add-Time-Zone hidden; commit URL shape,
include-local prepend, real-time-omits-`t`, and a **freeze round-trip** (open `&t=` link → Add restores
custom mode → re-share re-emits the same `t`); incoming filter/merge/dedupe/strip; frozen-time dialog;
dismiss is a no-op. Real-build screenshots re-passed both critics.

## Next

- Real-device QA of the share flow — native share sheet on iOS, the select-mode tap targets, the bar feel.
- Analytics: `share_started` / `share_committed` / `share_link_opened` / `share_link_added` are wired;
  confirm they land.
- Consider a `share_link_dismissed` event and a cap-reached toast when an incoming link would exceed 16.
