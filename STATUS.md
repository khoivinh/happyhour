# Happyhour — Status

Last updated: 2026-07-16

**Status:** In progress · **Live:** <https://happyhour.day> · **Stack:** Vite + React 18 + TS + Tailwind, static (Cloudflare Pages, auto-deploys on push) · **Repo:** `khoivinh/happyhour`

World clock + timezone converter (fka Khlock). No backend; weather via Open-Meteo (no API key).

## Current state
- 2026-07-16 (share-flow refinements, branch `share-flow-refinements`, **not yet merged**): on-device punch-list. Select mode no longer shifts layout — the grip hides into an `invisible` placeholder and the selection check moved into the ellipsis's exact flow slot (both left/right widths reserved). Unselected tiles lost the mid-tone (only selected keep the gold); the check is now **monochrome foreground** (black on light/Happy tiles, white on dark) instead of blue. Fixed the stuck-highlight bug (Share row now resets `isDropdownOpen`, which the unmounting menu Popover left stale). Esc cancels; "Add Time Zone" grays in place (was hidden → caused a jump); the drawer toggle grays + goes inert via a new `onShareModeChange` prop. Commit bar re-themed via `--share-bar-*` tokens — **black / yellow / white** for light / dark / Happy — with a full-contrast checkbox, and stacks on mobile. Hero + menu drop the region ("New York City"); menu widened 236→307px; Happy menu stroke softened. Tile padding rebalanced (top ≈ sides 10px, bottom slightly greater). 2-critic design loop → both `pass`; `tsc`/`build` clean; 35 Playwright assertions pass. See `docs/2026-07-16-devlog.md`.
- 2026-07-15 (share flow): the tile `…` icon now opens a **Share / Delete** menu instead of jumping to delete. Share enters a multi-select mode (check-badge tiles + a fixed bottom bar with an "Include my local time" checkbox, Cancel, and Share N); committing builds a `happyhour.day/?z=<cities>` link (local city prepended when checked; `&t=<epoch>` appended only in custom-time mode) and hands it to `navigator.share`, falling back to clipboard+toast on desktop. Opening such a link shows a preview/confirm `Dialog` ("Add {cities} to your clocks[ at {time}]?") that merges (dedupe, 16-cap) and restores the frozen custom time. New `components/share-selection-bar.tsx`; changes in `digital-clock.tsx`, `time-zone-converter.tsx`, `world-clock.tsx`, `index.css` (new `--tile-sel-*` tokens). Selection uses one control color across the flow (`primary`/`primary-foreground` → blue light/dark, black+yellow Happy). Design went through an Apple-caliber 2-critic loop to consensus (mockup + real build both passed); locked spec `scratchpad/share-design-spec.md`. Caught during verify: incoming links must resolve after `loadTopCities()`; dialog list uses bare city names (province commas fractured the sentence); `body` padding while active so the bar clears the footer. `tsc`/`build` clean; 26 Playwright assertions pass.
- 2026-07-15 (location hint): the footer's `geoDenied` notice now reads "Allow Happyhour to **access your location** for more precise local time and weather.", linking to a new **Support → Location access** section with per-browser (Safari/Chrome/Firefox) instructions + canonical help links. Chose Support instructions over a click-to-reprompt because JS can't reopen a hard-blocked prompt (and iOS Safari often never prompts on mount) — instructions work in every state. Key accuracy point: on iPhone, Chrome/Firefox inherit the OS location permission (Settings → Apps → …), no in-app per-site toggle. `site-footer.tsx` + `support.tsx` only; no geolocation code touched. 24 Playwright assertions pass.
- 2026-07-15: **onboarding tagline** — "Welcome to the indispensable world clock." (Zalando Black 36/38, −1px, 60% width, left-aligned to Add Time Zone) shows only for genuinely new visitors, gated on a persistent `world-happyhour-onboarded` flag (returning users with saved zones are silently onboarded; seeded tiles stay). First-ever add fades it and slides the button + grid up via a CSS grid `1fr→0fr` row collapse (no anim lib), respecting `prefers-reduced-motion`. All in `time-zone-converter.tsx`; 25 Playwright assertions pass. Mobile goes full-width (`w-full sm:w-3/5`) so 375px reads as 3 lines, desktop stays 60%/2 lines. Figma `338:3813`.
- 2026-07-14 (round 3): top baseline nudged **22px → 32px** (drawer icon, wordmark, sticky city name — they share one baseline by design), which pushes the menu panel off the viewport edge (**4px → 14px**). The panel's position is *derived* from the icon (`top: topOffset − 18`, matched by the panel's `pt-[18px]`, so its close icon lands on the toggle) — it can't be moved independently. Logo bar extracted to `components/logo-bar.tsx`: the padding was a copy-pasted literal in 3 files, and its "these match" comment had already gone stale twice. Sticky rule 115→125.
- 2026-07-14 (merged): **`alt-header` → `main`**, fast-forward, deployed to production. Merge caught one straggler: the ancillary headers (About/Privacy/Support/404) still had round 1's `pt-[29px]` while home moved to `pt-[13px]` in round 2 — a 16px jump on navigation, under a comment promising there was none. Fixed and measured: wordmark ink top is 22px on all five pages at both breakpoints (`b2daa7f`).
- 2026-07-14 (round 2): **`alt-header`** — feedback fixes. The scroll "shudder" turned out to be **browser scroll anchoring**, not the document-height feedback I first diagnosed: the hero's shrink made Chrome nudge `scrollTop`, which drives the shrink, which re-triggered the nudge. `html { overflow-anchor: none }` + a `min-h-[calc(100lvh+120px)]` scroll runway. Ratio now reaches exactly 1 at the reported repro (was stuck at 0.56, oscillating). Also: geo notice moved to the footer, hero tap target narrowed off the drawer icon, sticky zone/temp → 12px, icon top → 22px (aligns with the city name when sticky and the wordmark at top).
- 2026-07-14: **`alt-header` branch (not merged)** — logo bar + hero clock now read as one unit. Header de-stuck and de-ruled, mark moved right of the wordmark; hero shrinks continuously on scroll (96→36px) and its rule locks to the top of the viewport, so only one rule is ever visible. Drawer toggle is now genuinely pinned (it previously drifted ~7.5px) — measured identical at every scroll offset, Δ0.00px against the sidebar close icon. Figma `329:3241` / `329:3521`. See `docs/2026-07-14-devlog.md`.
- 2026-06-14: fixed Safari/macOS slashed zeros in the time-edit input (`<input type=time>` uses tabular figures; Zalando Sans's tabular `0` is slashed). Pinned `proportional-nums` + disabled the `zero` feature in `digital-clock.tsx`. `tsc --noEmit` clean.
- 2026-06-02: fixed sub-hour relative offset (India UTC+5:30 was rounding to whole hours via `Math.round`) in `time-zone-converter.tsx` + `digital-clock.tsx`. `tsc --noEmit` clean.
- 2026-04-25 polish batch shipped: cookie banner/modal fixes, ancillary-header parity with the home header, copy + link cleanup, mobile favicon + `manifest.json`, and a theme-color status-bar fix (per-theme tint rewritten in `theme-provider.tsx`).

## Next steps
- Real-device QA of the share flow: iOS native share sheet, select-mode tap targets, bar feel; confirm the `share_*` analytics events land
- Real-device QA of the onboarding tagline fade + reflow (feel, not screenshot)
- Confirm the prod deploy of the new header on happyhour.day (Cloudflare builds on push to `main`)
- Real-device QA on the scroll shrink — it's a feel thing, not a screenshot thing
- Browser/device QA on the visual polish
- Verify iOS home-screen icon + Android Chrome tab favicon on real devices
- Device check of mobile drag (dnd-kit)
- Playwright coverage on the cookie flow + new routes (About / Privacy / Support)
- Mobile header alignment tweak

## Pointers
- Key files: `client/src/pages/world-clock.tsx`, `components/digital-clock.tsx`, `components/time-zone-converter.tsx`, `components/logo-bar.tsx`, `components/share-selection-bar.tsx`
- Share flow: link schema is `/?z=<comma city keys>[&t=<epoch ms>]`; `t` is set only in custom-time mode. Incoming parse lives in `world-clock.tsx` (resolves after `loadTopCities()`); commit + select-mode state in `time-zone-converter.tsx` (reports up via `onShareModeChange`); menu + tile selection in `digital-clock.tsx`. Selection check = `--foreground` (monochrome); selected-tile fill = `--tile-sel-*`; commit bar surface = `--share-bar-*` (black/yellow/white per theme)
- Header geometry is one interlocked system: `TOGGLE_TOP` (world-clock.tsx) sets the drawer icon **and** the menu panel; `LogoBar`'s padding and `.hero-clock`'s padding-top must land on the same baseline. Change one, re-measure all four.
- Design doc: `docs/PRD.md` · Devlogs: `docs/` (latest 2026-07-16)
- Share flow is live on `main`; **`share-flow-refinements` is the current working branch, not yet merged** (awaiting Khoi's go-ahead). Branch previews live at `<branch>.khlock.pages.dev` — the Pages project is still named `khlock`, so it is *not* a `happyhour.day` subdomain.
